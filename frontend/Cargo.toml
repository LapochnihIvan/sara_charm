[package]
name = "sara_charm_frontend"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = true
panic = "abort"
strip = true
opt-level = "z"

[dependencies]
yew = { version = "0.22.0", features = ["csr"] }
